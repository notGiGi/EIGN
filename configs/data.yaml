data:
  # Training data directory
  # - Leave null to use CLI --train-data-dir (recommended for Kaggle)
  # - Set explicit path for local development
  train_dir: null
  tokenizer_model: artifacts/tokenizer/v0001/eign_spm_unigram_32k.model
  cache_dir: data/tokenized
  seq_len: 512
  shuffle: true
  seed: 42
